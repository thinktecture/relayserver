import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'thinktecture-relay-server-management-frontend',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
